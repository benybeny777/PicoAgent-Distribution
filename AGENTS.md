# AGENTS.md — PicoAgent配布リポジトリ作業ガイド

このファイルを唯一のルール正本とする。`CLAUDE.md`はこのファイルを読むための入口だけにする。

## 運用ルール

- すべての変更は`origin/main`から目的別ブランチを作り、PRを作成してsquash mergeする。`main`へ直接pushしない。
- 本体リポジトリの`distribution/site/`をLP・配布文書・`latest.json`の正本とし、公開時に追跡対象ファイルを完全一致させる。
- Release資産は同じ版へ上書きしない。通常更新はpatchを上げ、minor／majorはユーザーの明示指示がある場合だけ上げる。
- ベータ版は利用者へ完全な版番号と`Beta`を表示する。patch更新はアプリの起動時に通知せず、手動確認では取得可能にする。minor／major更新は起動時にも通知する。
- `latest.json`の版、URL、バイト数、SHA-256は公開する実ファイルから生成し、推測や手入力で合わせない。
- 公開前にLPをPC幅とスマートフォン幅の実ブラウザーで確認する。横はみ出し、画像欠落、古い説明、古い取得先を残さない。
- 画像はユーザーが差し替えを求めた場合だけ変更する。画像を変更しない公開では、既存画像のハッシュが変わっていないことを確認する。
- 新しいReleaseを作成してからPagesを公開し、Pagesの対象コミット、公開`latest.json`、Release資産の外部再取得ハッシュを照合する。
- 古いインストーラ、検証用ダウンロード、ログ、一時クローン、`.part`、`.incoming`を残さない。削除対象はPicoAgent管理下または作業用`temp/`内の正規化済みパスだけに限定する。
- CodexとClaudeは互いを自動起動しない。相互レビューはユーザーが同一PRについて明示した場合だけ行う。
- レビューと公開手順には`.agents/skills/publish-picoagent-distribution/SKILL.md`を使う。再利用手順を会話履歴から組み直さない。
